# 採らなかった案

検討して見送った設計の記録。同じ議論を繰り返さないために残す。
移植状況は [PORTING.md](PORTING.md)、verify の記録は [VERIFY.md](VERIFY.md) を参照。

## avl_segtree: 葉を配列にする

葉に「任意の値を最大 B 個」持たせて節点数を 1/B にする案。

同じ値をまとめて持つ今の形（`insert(i, x, 10^9)` が節点 1 個）を失うため採らない。
区間作用のたびに葉が割れるので、痩せた葉を併合し直す管理も別途必要になる。

## avl_segtree: 平衡を RBST（乱択）にする

回転と平衡を捨てて、つなぎ目を乱択で決める案。API を同じにした試作を作り、
同じテストと Library Checker 2 問を通したうえで測った。

- **行数はほとんど減らない**（20 行ほど）。回転と平衡が消える代わりに、
  乱数と、葉の数を数えるフィールドが要る
- **節点はむしろ大きくなる**。連長圧縮した葉があるぶん「要素数」と「葉の数」が
  別物なので、1 バイトの高さ `rnk` を消しても、葉の数を数える 4 バイトが要る
- 速度は機能ごとに勝ち負けが分かれ、Library Checker 2 問の合計では AVL が
  4〜9% 速い。挿入位置がランダムな `insert` は AVL が 1.9 倍速い
  （RBST 版は split + merge の 3 往復になるため）
- 最悪計算量の保証を期待計算量に落とすことになる

## avl_segtree: 平衡を重み平衡木にする

**原理的に載らない。** 重み平衡木は左右の部分木の要素数の比で平衡を判定するが、
連長圧縮した葉は分割できないので、要素数 10^9 の葉 1 枚の隣に 1 要素を置いた
時点で比が 10^9 : 1 になり、回転では直せない。

「重み＝節点数」と定義し直せば回避できるが、要素数とは別のカウンタを持つことに
なり、`rnk` を消した意味が無くなる。連長圧縮した葉を持つ以上、平衡は高さか乱数で
取るしかない。
